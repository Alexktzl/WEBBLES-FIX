# cantools/textparser  [FAIL]

- Дата: 2026-06-20T12:12
- Статус: KILLED (принудительно остановлен вручную)
- ACCEPT: 0 | NEEDS_REVIEW: 0 | REJECT: 0
- Причина: зависание локального LLM-провайдера — 20+ `LLM hard timeout #N: 180s exceeded`
  подряд без единого успешного ответа, ~45+ минут без прогресса. `project_timeout` (600s)
  не успел сработать, т.к. внутренняя проверка идёт между циклами, а зависание произошло
  внутри одного LLM-вызова с цепочкой ретраев (10s→30s→60s backoff).
- Действие: Stop-Process на оба `python.exe run_agent.py` PID. `webles_config.json`
  восстановился сам через bash EXIT trap (когда дочерний python завершился).

## Важно
LLM-сервер (`localhost:8080`) остался живым и отвечал на `/v1/models` всё это время —
зависание специфично для конкретных generate-запросов на этом проекте, не падение сервиса
целиком. На двух предыдущих проектах серии LLM отвечал (хоть и с собственными таймаутами).
**Не воспроизводилось ни на одном из прошлых control series** — кандидат на отдельное
расследование локальной LLM-инфраструктуры, НЕ регрессия от закрытого techdebt backlog.
