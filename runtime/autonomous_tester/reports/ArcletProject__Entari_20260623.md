# ArcletProject/Entari  [PASS, верифицировано]

- Дата: 2026-06-23T11:27:41
- Ошибок: 237  →  213  (delta=24)
- ACCEPT (заявлено): 4  |  NEEDS_REVIEW: 115  |  REJECT: 2
- **Верификация (git show HEAD + свежий рескан): 1 REAL_FIX, 3 STILL_FLAGGED**

## Детали верификации
1. `arclet/entari/__init__.py:1` import-not-found — **STILL_FLAGGED**. Патч добавил `# type: ignore[import-not-found]` к ОДНОЙ строке импорта (`use`), но 4 соседние строки импорта из того же модуля (`Depends`, `bind`, `depends`, `deref`) остались без подавления — свежий скан всё ещё находит import-not-found в этом файле рядом со строкой 1.
2. То же самое ACCEPT встретилось дважды (повторная попытка на той же строке) — оба раза STILL_FLAGGED.
3. `arclet/entari/config/models/default.py:169` F821 'fields' — **REAL_FIX**, подтверждено.
4. `arclet/entari/config/models/default.py:201` F821 'fields' — **STILL_FLAGGED**, тот же класс ошибки на соседней строке не тронут.

**Реальная доля подтверждённых фиксов: 1/4 = 25%** — система засчитала 4 ACCEPT, но независимая проверка подтверждает только 1.

## unsafe_accept (ручная оценка — клон удалён до появления этой категории, алгоритмически не пересчитано)
Пункты 1/2/4 (import-not-found) — до/после показывают, что ОРИГИНАЛЬНЫЕ 4 строки импорта (`Depends`/`bind`/`depends`/`deref` из `arclet.letoderea`) полностью исчезли, заменены ОДНОЙ новой строкой импорта `use`. Это в точности паттерн `ruff --select=F401`, независимо подтверждённый на malinkang/toggl2notion (см. отдельный отчёт) — **высокая уверенность в unsafe_accept=True** для этих 3 записей, хотя алгоритмически (`check_symbol_regression`) не перепроверено, так как клон уже был удалён к моменту добавления этой проверки. Пункт 3 (F821 'fields', REAL_FIX) — диф показывал только реорганизацию тела функции, без признаков потери импортов; unsafe_accept не подозревается, но также не перепроверено алгоритмически.

**После фикса (F401 убран из RuffAutoFixStage._SAFE_CODES) этот класс находок на новых прогонах не должен повторяться** — подтверждено отдельным повторным прогоном на malinkang/toggl2notion (0 unsafe_accept после фикса).
