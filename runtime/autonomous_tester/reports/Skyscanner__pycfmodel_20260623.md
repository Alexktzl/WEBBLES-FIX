# Skyscanner/pycfmodel  [PASS, верифицировано]

- Дата: 2026-06-23T15:47:20
- Ошибок: 298  →  1  (delta=297)
- ACCEPT (заявлено): 11  |  NEEDS_REVIEW: 1  |  REJECT: 2
- **Верификация: 4 REAL_FIX, 7 STILL_FLAGGED, 0 unsafe_accept**

## REAL_FIX (4)
- `resolver.py:36` union-attr — заменил `.match().group()` на присвоение в переменную с проверкой `if match:`. Чистый фикс.
- `resolver.py:66` operator — добавил `Any`-аннотацию переменной-резолверу + isinstance-проверку. Реально устранил ошибку, но через типовую эрозию (`Any`), не через точное сужение типа.
- `resolver.py:69` assignment — аннотация `Dict[Any, Any]`. Та же типовая эрозия.
- `types.py:43` return-value — поменял возвращаемый тип метода с `IPv4Network` на `Any`. Технически устраняет несовпадение, но `Any` маскирует, а не решает реальную типовую несовместимость.

**Наблюдение**: 3 из 4 REAL_FIX достигнуты через ослабление типа до `Any`, а не через содержательную правку. Формально ошибка ушла, содержательно — типобезопасность снижена, не повышена.

## STILL_FLAGGED (7) — причины
- `test_statement.py:119,121` arg-type — заменил `Statement(**{...})` на `Statement(Effect=..., Action=..., Resource=...)` (распаковку kwargs на явные именованные аргументы), но mypy всё равно видит несовместимость аргументов в одном из вызовов (паттерн воспроизведён частично, не везде).
- `test_ec2_vpc_endpoint_policy.py:51,52,53` union-attr — добавил `is not None`-проверку и промежуточную переменную `policy_doc: PolicyDocument`, реальное улучшение типобезопасности видно в диффе, но рескан всё равно находит ошибку (вероятно на новой строке рядом — типовое сужение не закрывает весь сценарий использования).
- `policy_document.py:137,138` attr-defined — добавил `isinstance(statement, Statement)`-проверку, но **попутно переименовал `.Effect` → `.effect`** (смена регистра) без явной связи с устраняемой ошибкой. Подозрение на риск поведения (Pydantic-поле может называться именно `Effect`, новый `.effect` может не существовать вовсе) — отдельный класс риска, не покрытый текущей защитой symbol_regression (это не пропавшее имя, а **изменённое использование атрибута**, симптоматически похоже на "лечит один симптом, создаёт другой").
