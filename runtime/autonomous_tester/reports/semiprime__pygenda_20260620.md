# semiprime/pygenda  [FAIL]

- Дата: 2026-06-20T11:22
- Ошибок: 7134  →  7134 (delta=0, current_total_errors=16138)
- ACCEPT: 0  |  NEEDS_REVIEW: 0  |  REJECT: 0
- Score: 1.0
- Стоп: project_timeout (600s, py_count=26), статус FAILED, 0 циклов выполнено

## НОВАЯ ПРОБЛЕМА (не зафиксирована в прежних аудитах)
`PreCleanupStage` сделал `pygenda/pygenda_calendar.py` и `pygenda/pygenda_dialog_event.py`
синтаксически невалидными при попытке применить детерминированное правило
(safety-check поймал это и пропустил запись на диск — реальные файлы НЕ повреждены),
но каждая повторная попытка всё равно засчитывалась `FileAntiLoop`, который быстро
дошёл до лимита (21/20) для обоих файлов. В результате весь project_timeout (600s)
был потрачен на бесполезные повторы PreCleanup, и движок не успел сделать ни одного
полезного цикла анализа/патчинга — несмотря на то что в логе видно как минимум один
валидный structured_llm-патч (E275), он не успел дойти до DECIDE.

Также подозрительно: current_total_errors (16138) почти вдвое больше initial_error_count
(7134) — возможно тот же эффект PreCleanup-петли создаёт лишний шум, либо проект
действительно генерирует много mypy/security находок на typed GTK-коде. Не расследовал
глубже — отдельная задача для бэклога, не входила в scope контрольной серии.

**Не регрессия от закрытых в этой сессии фиксов** (F4/F5/F6/F821/E704/E127-E128/W391/W503/
process_key) — это независимый, ранее неизвестный баг в `PreCleanupStage` на нетипичном
codebase (GTK-приложение с большим количеством PyGObject-вызовов).
