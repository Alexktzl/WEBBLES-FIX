# dfdezdom/investdaytip  [FAIL]

- Дата: 2026-06-20T12:58
- Статус: KILLED (принудительно остановлен после ~40+ минут при бюджете 720s)
- ACCEPT: 0 | NEEDS_REVIEW: 0 | REJECT: 1 (видимый, до остановки)

## НОВАЯ ПРОБЛЕМА: project_timeout не enforced надёжно
Десятки последовательных попыток патчить `src/investdaytip/html_export.py` ломали синтаксис
(unexpected indent / unindent does not match outer level) на разных строках файла
(82 → 521), каждая безопасно откатывалась (реальные файлы не повреждены — invalid-syntax
guard работал корректно). Но адаптивный `project_timeout` (720s для 36 файлов) не остановил
процесс — реальное время работы составило ~40+ минут, более чем 3-кратное превышение
бюджета, прежде чем потребовалось ручное `Stop-Process`.

В отличие от cantools/textparser (зависший LLM-вызов без какой-либо активности),
здесь была РЕАЛЬНАЯ непрерывная активность движка (decide/apply/rollback) — то есть это
не зависание провайдера, а структурная проблема контроля бюджета: похоже, что
`project_timeout`/`AdaptiveCycleController` проверяет бюджет только между полными циклами
по ошибке, а не внутри retry-цикла одной и той же ошибки/файла. Если retry-цикл сам не
ограничен по времени независимо, общий бюджет можно превысить многократно.

**Рекомендация для backlog:** добавить wall-clock проверку project_timeout НЕ только между
циклами, но и внутри retry-петли одной ошибки (например, каждые N попыток или каждые M секунд
реального времени внутри `_blocking_adaptive_strategy_no_sandbox`/аналогичных retry-циклов).
