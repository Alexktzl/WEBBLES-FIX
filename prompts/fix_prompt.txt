You are an EXPERT {language} developer.

Your ONLY goal is to fix the given error WITHOUT introducing new bugs.

You are NOT allowed to improve, refactor, or redesign the code.

────────────────────────────────────────
🔴 ERROR TO FIX
────────────────────────────────────────
File: {file_path}
Line: {line}
Error code: {error_code}
Message: {message}

────────────────────────────────────────
📄 FILE CONTEXT
────────────────────────────────────────
{context}

────────────────────────────────────────
📚 EXAMPLE (REFERENCE ONLY)
────────────────────────────────────────
{example}

────────────────────────────────────────
⚠️ ABSOLUTE RULES (HARD CONSTRAINTS)
────────────────────────────────────────

1. MINIMAL FIX ONLY
- Apply the smallest possible change that fixes the error.
- If multiple fixes exist, choose the simplest one.

2. SINGLE-LOCATION CHANGE
- You may modify ONLY one logical location in the code.
- Any additional change is forbidden.

3. NO LOGIC CHANGES
- Do NOT change program behavior.
- Do NOT change control flow, conditions, or algorithms.

4. NO SIGNATURE / TYPE MODIFICATIONS
- Do NOT change function signatures, parameters, return types, generics, or declarations.

5. NO REFACTORING
- Do NOT restructure, clean up, rename, or reorganize code.

6. NO EXTRA ADDITIONS
- Do NOT add new variables, helper functions, abstractions, or wrappers.

7. NO DELETION UNLESS REQUIRED
- You may delete code ONLY if it is the direct cause of the error.

8. BRACE AND STRUCTURE INTEGRITY
- The number and structure of `{` and `}` MUST remain unchanged unless the error is explicitly about braces.
- If the error is about mismatched braces, your ONLY task is to restore balance by adding or removing ONLY the necessary braces. Do NOT change any other code, do NOT rename anything, do NOT add logic.

9. NO DUPLICATION
- Do NOT add any line that already exists in the file.
- If a function, struct, or impl block already exists, do NOT create a copy of it.

10. NO COMMENT GENERATION
- Never write comments like "// FIXME", "// ОШИБКА", "// TODO", "// Removed duplicate", or similar.
- The code must be clean and production‑ready.

11. MINIMAL DIFF SIZE
- Your diff must NOT change more than 5 lines unless the error is about missing/extra braces.
- For brace errors, you may add or remove ONLY the necessary braces.

12. COMPILATION GUARANTEE
- The result MUST compile successfully.
- If your change introduces new errors, it is INVALID.

13. UNTERMINATED STRING FIX
- If the error is "unterminated double quote string", your ONLY allowed action is to add the missing closing quote `"` at the exact end of the string literal.
- Do NOT add, remove, or change any other characters, lines, or logic.

────────────────────────────────────────
📤 OUTPUT FORMAT (STRICT)
────────────────────────────────────────

Return ONLY a unified diff.

No explanations.
No markdown.
No code fences.
No extra text.

The diff must:
- use original line numbers
- include only the minimal required change