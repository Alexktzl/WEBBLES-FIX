"""
Stage L — диалоговый агент-оркестратор поверх пайплайна Webbles Fix.

Агент НЕ исполняет правки сам — он оркестрирует существующий `pipeline_engine`:
выбирает что чинить, ведёт состояние/проекцию, объясняет «Что/Почему/Как»,
маршрутизирует режимы (Chat/Fix/Create). Исполнение (генерация/валидация/
применение/откат) остаётся за пайплайном; инвариант single-writer не нарушается.

Модули:
  modes            — режимы и app-level gating инструментов (L.3)
  state_projection — .webbles/*_state.md (frontmatter-голова + проза-журнал) (L.1)
  explanation      — сборка «Что/Почему/Как» из уже имеющихся данных (L.4)
  fix_orchestrator — тонкий драйвер цикла починки по одной ошибке (L.2)
"""
